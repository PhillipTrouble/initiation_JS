// Je verifie ma connection au fichier html
alert("okay js");

/****************************
 les operateurs arithmétiques
*****************************/

//1 Déclaration et affectation de plusieurs variables :

var nb1, nb2, resultat;

nb1 = 10;
nb2 = 5;

// L'addition de nb1 & nb2 avec L'operateur " + "
        resultat = nb1 + nb2;
        console.log(resultat);

// La soustraction de nb1 et nb2 avec L'opérateur " - "
        resultat = nb1 - nb2;
        console.log(resultat);

// La division de nb1 & nb2 avec L'opérateur " - "
        resultat = nb1 / nb2;
        console.log(resultat);

// La multiplication de nb1 & nb2 avec L'opérateur " * "
        resultat = nb1 * nb2;
        console.log(resultat);

/*
Le modulo correspond au reste de la division.
Le modulo de nb1 & nb2 avec L'opérateur " % "
*/
        resultat = nb1 * nb2;
        console.log(resultat);

/**************************
 LES ÉCRITURES SIMPLIFIÉES
***************************/

// Addition

// je crée ma variable nb1 et je lui affècte une valeur 15 (avec Le symbole =)
        nb1 = 15;
// première variante :
// pour ajouter 5 à ma valeur de 15, je réaffecte à ma variable une nouvelle instruction :
// je lui dit que ma variante nb1 sera maintenant le résultat de ma valeur de base (donc 15) auquel je lui rajoute 5 :
/*      ma variable      La valeur de réference
            nb1 =                  nb1                +    5; 
        ici on dit que le résultat final de nb1 est la somme de notre variable +5 */

        console.log(nb1+5);
        console.log(typeof nb1);

        console.log(nb1);//retourne 20 donc 15 + 5

// deuxième variante
nb1 += 5; // ce qui est équivalent à écrire nb1 = nb1 + 5
        console.log(nb1);

/*
    je peux procéder de la même manière pour tous les autres opérateurs arithmétiques :
    " + ", " - ", " * ", " / " et " % "

*/
nb1 += 5;
        











